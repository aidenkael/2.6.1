# Contracts

当前新增：

- `agent_calibration_rule_package_v1.schema.json`：Agent → 软件物流校准规则包 V1 JSON Schema。
- `agent_calibration_rule_package_v1.example.json`：合法 candidate 示例。

详细设计与边界见：

- `../agent_calibration_rule_package_v1.md`

注意：V1 当前仅为合同规范，尚未接入 `CalibrationManager`、validator 或 offline replay。
