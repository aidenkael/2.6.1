# 项目工作约定

## 当前目标

- 完成真实视觉模型接入：AI 识图必须能按图片框类型读取图片，并将可确认的商品、材质、尺寸、重量、成本和包装候选回填到对应字段。
- 按用户最新意见修改主界面与设置界面 UI。
- 完成与上述目标直接相关的小修正，不擅自重构或扩展业务范围。

## 决策优先级

1. 用户最新修改意见
2. `docs/UI_LOCK.md` 中的 1920×1080 目标截图和验收规则
3. SVG 设计稿
4. 现有代码

## 实施规则

- 开发和验收时同时以实际普通用户视角审视功能是否智能、便利、实用：优先减少重复操作、专业术语和不必要的选择；发现功能虽可用但不直观、不能自动完成主要任务或容易误导时，先提出具体改进意见。
- 对用户的设想必须客观、科学地分析，不为照顾面子而附和；明确说明可行性、必要性、边界、风险与更合适的替代方案。
- 先检查现有代码、文档、配置、测试和当前改动；不重复实现已有功能。
- AI 未配置、接口失败或图片证据不足时，明确提示并保留人工填写；不得伪造识别结果。
- AI 返回值与本地包装估算、物流和利润计算分层：AI 只提供识别事实和候选，费用与利润仍由本地确定性逻辑计算。
- 不修改已确认字段名称、业务公式、货代规则或历史记录结构，除非用户明确要求。
- UI 不重新设计、不自行美化；所有控件显式设置尺寸、间距、边距和样式，不能依赖 Qt 默认值。
- 固定验收为 Windows 1920×1080、100% 缩放、软件最大化：主页面无滚动条；缩小窗口时保持 UI 尺寸并出现滚动条，不机械压缩。
- 每次 UI 修改后生成实际运行截图，与目标图并排比较；视觉未达标不得打包。
- 修改后运行与改动相称的测试，并如实说明已验证、未验证和风险。
